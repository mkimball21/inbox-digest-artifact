{
  "name": "inbox-digest-test-harness",
  "private": true,
  "description": "Verification harness for inbox-digest.jsx (not part of the artifact itself). See test/README.md.",
  "scripts": {
    "live-verify": "node live-verify.mjs",
    "live-calibrate": "node live-calibrate.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  }
}
